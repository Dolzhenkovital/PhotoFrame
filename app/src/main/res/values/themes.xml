<resources>
    <!-- Fullscreen black theme: photos live on a black canvas, and a dark
         window background is also the cheapest thing an old GPU can draw. -->
    <style name="Theme.PhotoFrame" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
